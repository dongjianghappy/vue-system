const mutations = {
  setChannel: (state: any, params: any) => {
    state.channel = params
  }, 
  setPage: (state: any, params: any) => {
    debugger
    state.page = params
  },   
}

export default mutations
