const mutations = {
  setGraphType: (state: any, params: any) => {
    state.graphType = params
  },
  setNodeData: (state: any, params: any) => {
    state.nodeData = params
  }
}

export default mutations
